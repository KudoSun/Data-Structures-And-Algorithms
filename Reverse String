#include <iostream>
using namespace std;
int main() {
    string s;
    cin>>s;
    cout<<s<<endl;
    for(int i=0;2*i<s.length();i++){
        swap(s[i],s[s.length()-i-1]);
    }
    cout<<s<<endl;
    return 0;
}
